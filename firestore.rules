rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Anyone can read a map, but only the owner can write to it
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
